# chat provider adapters (Phase 2)

> **Status: empty (Phase 2 work).**
>
> This directory will hold concrete provider implementations:

- `slack.py` — wraps `korotovsky/slack-mcp-server`
- `teams.py` — community (later)
- `discord.py` — community (later)

All implement `qa_cortex.providers.ChatProvider` Protocol.
