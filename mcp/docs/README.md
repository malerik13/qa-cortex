# docs provider adapters (Phase 2)

> **Status: empty (Phase 2 work).**
>
> This directory will hold concrete provider implementations:

- `confluence.py` — wraps `sooperset/mcp-atlassian` (Confluence subset)
- `notion.py` — community (later)
- `github_wiki.py` — gh CLI wrapper (later)

All implement `qa_cortex.providers.DocumentationProvider` Protocol.
