# test-mgmt provider adapters (Phase 2)

> **Status: empty (Phase 2 work).**
>
> This directory will hold concrete provider implementations:

- `testrail.py` — wraps `bun913/mcp-testrail`
- `zephyr.py` — community (later)
- `xray.py` — community (later)
- `allure.py` — kept in scalefinal-qa-assistant

All implement `qa_cortex.providers.TestManagementProvider` Protocol.
